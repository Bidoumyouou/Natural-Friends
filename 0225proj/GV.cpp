
#include "GV.h"
#include<DxLib.h>
unsigned int RED         = GetColor(255, 0, 0);
unsigned int BLUE       = GetColor(0, 0, 255);
unsigned int GREEN       = GetColor(0, 255, 0);
unsigned int WHITE       = GetColor(255, 255, 255);
unsigned int BLACK       = GetColor(0, 0, 0); 
unsigned int GRAY        = GetColor(140, 140, 140);

unsigned int YELLOW     = GetColor(255, 255, 0);
unsigned int LIGHT_BLUE = GetColor(0, 255, 255);
unsigned int PINK       = GetColor(255, 0, 255);

unsigned int ORANGE     = GetColor(255, 128, 0);
unsigned int LIGHT_GREEN = GetColor(0, 255, 128);
unsigned int PURPLE     = GetColor(128, 0, 255);

















